name: Finalize DATA SEARCH navigation

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/finalize-navigation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  finalize-navigation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout published site
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Replace conflicting navigation patches
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import re

          path = Path('data-search.html')
          html = path.read_text(encoding='utf-8')

          for start, end in [
              ('NAVIGATION_LAYOUT_V3_START', 'NAVIGATION_LAYOUT_V3_END'),
              ('DATA_ACCOUNT_NAV_FIX_START', 'DATA_ACCOUNT_NAV_FIX_END'),
              ('FINAL_NAVIGATION_FIX_START', 'FINAL_NAVIGATION_FIX_END'),
          ]:
              html = re.sub(
                  rf'\n?<!-- {start} -->.*?<!-- {end} -->\n?',
                  '\n',
                  html,
                  flags=re.S,
              )

          patch = r'''<!-- FINAL_NAVIGATION_FIX_START -->
<style id="final-navigation-fix-style">
:root{--ds-rail-width:86px}
.app{grid-template-columns:var(--ds-rail-width) 336px minmax(0,1fr)!important}
.app.v11-order-full,.app.account-center-workspace{grid-template-columns:var(--ds-rail-width) minmax(0,1fr) 0!important}
.brand{width:calc(var(--ds-rail-width) + 336px)!important}
.coordinate-search{display:none!important}
.top-actions>:not(.v11-auth-actions){display:none!important}
.top-actions .v11-auth-actions{display:flex!important;align-items:center!important;gap:7px!important}
.top-actions .v11-auth-actions>:not(#v11LoginBtn):not(#v11LogoutBtn){display:none!important}
.top-actions #v11LoginBtn,.top-actions #v11LogoutBtn{display:inline-flex!important;align-items:center!important;justify-content:center!important;min-width:68px!important}
.rail{align-items:stretch!important;padding:8px 6px!important;overflow-x:hidden!important;overflow-y:auto!important}
.rail::-webkit-scrollbar{width:0}
.rail-btn{width:72px!important;height:58px!important;min-height:58px!important;margin:2px auto!important;padding:5px 3px!important;display:flex!important;flex-direction:column!important;align-items:center!important;justify-content:center!important;gap:5px!important;line-height:1!important}
.rail-btn .ds-icon{width:19px!important;height:19px!important;flex:0 0 19px!important}
.rail-btn .tip{position:static!important;left:auto!important;display:block!important;width:100%!important;min-height:15px!important;padding:0!important;background:transparent!important;color:inherit!important;border-radius:0!important;box-shadow:none!important;opacity:1!important;transform:none!important;white-space:normal!important;text-align:center!important;font-size:7px!important;font-weight:700!important;line-height:1.12!important;pointer-events:none!important}
.rail-btn:hover .tip{opacity:1!important;transform:none!important}
.rail-btn.active:before{left:-6px!important;height:34px!important}
.rail-bottom{width:100%!important;margin-top:auto!important;display:flex!important;flex-direction:column!important;align-items:center!important}
@media(max-height:700px){.rail-btn{height:52px!important;min-height:52px!important;gap:3px!important}.rail-btn .tip{font-size:6.5px!important}}
</style>
<script id="final-navigation-fix-script">
(function(){
  function setLabel(button,label){
    if(!button)return;
    button.title=label;
    button.setAttribute('aria-label',label);
    var tip=button.querySelector('.tip');
    if(!tip){tip=document.createElement('span');tip.className='tip';button.appendChild(tip);}
    tip.textContent=label;
  }
  function setActive(button){
    document.querySelectorAll('.rail .rail-btn').forEach(function(item){item.classList.remove('active');});
    if(button)button.classList.add('active');
  }
  function fallbackPanel(name){
    document.querySelectorAll('.panel-view').forEach(function(panel){panel.classList.toggle('active',panel.id==='panel-'+name);});
  }
  function openPanel(name,button){
    try{
      if(typeof window.selectPanel==='function')window.selectPanel(name);
      else fallbackPanel(name);
    }catch(error){fallbackPanel(name);}
    setActive(button);
    if(name!=='orders'&&name!=='account'&&window.map&&typeof window.map.invalidateSize==='function')setTimeout(function(){window.map.invalidateSize();},80);
  }
  function openSearchTab(tab,button){
    try{
      if(typeof window.selectPanel==='function')window.selectPanel('search');
      else fallbackPanel('search');
    }catch(error){fallbackPanel('search');}
    try{if(typeof window.setSearchTab==='function')window.setSearchTab(tab);}catch(error){}
    var tabs=document.querySelector('#panel-search .tabs');
    if(tabs){
      var aoi=tabs.querySelector('[data-tab="aoi"]');
      var archive=tabs.querySelector('[data-tab="archive"]');
      if(tab==='aoi')tabs.style.display='none';
      else{
        tabs.style.display='flex';
        if(aoi)aoi.style.display='none';
        if(archive){archive.style.display='';archive.textContent='Archive Search';}
      }
    }
    setActive(button);
    if(window.map&&typeof window.map.invalidateSize==='function')setTimeout(function(){window.map.invalidateSize();},80);
  }
  function install(){
    document.querySelector('.coordinate-search')?.remove();
    var rail=document.querySelector('.rail');
    if(!rail)return;

    var aoi=rail.querySelector('[data-ds-module="aoi"]');
    var archive=rail.querySelector('[data-ds-module="archive"]');
    var tasking=rail.querySelector('.rail-btn[data-panel="orbit"]');
    var order=rail.querySelector('.rail-btn[data-panel="orders"]');
    var account=rail.querySelector('.rail-btn[data-panel="account"]');
    var data=rail.querySelector('.rail-btn[data-panel="data"]');

    setLabel(aoi,'AOI');
    setLabel(archive,'Archive Search');
    setLabel(tasking,'Tasking');
    setLabel(order,'Order');
    setLabel(account,'Account');
    setLabel(data,'Data');

    var taskTitle=document.querySelector('#panel-orbit .panel-title');
    if(taskTitle)taskTitle.textContent='Tasking';

    var bottom=rail.querySelector('.rail-bottom');
    if(!bottom){bottom=document.createElement('div');bottom.className='rail-bottom';rail.appendChild(bottom);}
    var bottomButtons=Array.prototype.slice.call(bottom.querySelectorAll('.rail-btn'));
    var settings=bottomButtons.find(function(button){
      var text=((button.querySelector('.tip')||{}).textContent||button.title||'').trim();
      return /support|settings|help/i.test(text);
    })||bottomButtons[bottomButtons.length-1];
    var language=bottomButtons.find(function(button){
      var text=((button.querySelector('.tip')||{}).textContent||button.title||'').trim();
      return /language/i.test(text);
    });
    if(settings){settings.dataset.navAction='settings';setLabel(settings,'Settings');}
    if(language){language.dataset.navAction='language';setLabel(language,'Language');}
    if(data){if(settings)bottom.insertBefore(data,settings);else bottom.appendChild(data);}

    rail.addEventListener('click',function(event){
      var button=event.target.closest('.rail-btn');
      if(!button||!rail.contains(button))return;
      event.preventDefault();
      event.stopImmediatePropagation();

      if(button.matches('[data-ds-module="aoi"]'))return openSearchTab('aoi',button);
      if(button.matches('[data-ds-module="archive"]'))return openSearchTab('archive',button);
      if(button.dataset.panel==='orbit')return openPanel('orbit',button);
      if(button.dataset.panel==='orders')return openPanel('orders',button);
      if(button.dataset.panel==='account')return openPanel('account',button);
      if(button.dataset.panel==='data')return openPanel('data',button);
      if(button.dataset.navAction==='settings'){
        if(typeof window.openPlatformSettings==='function')window.openPlatformSettings('identity');
        return;
      }
      if(button.dataset.navAction==='language'){
        var topLanguage=document.getElementById('languageToggle');
        if(topLanguage&&topLanguage!==button)topLanguage.click();
      }
    },true);
  }
  function start(){install();setTimeout(install,120);setTimeout(install,500);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});
  else start();
  window.addEventListener('load',function(){setTimeout(install,0);},{once:true});
})();
</script>
<!-- FINAL_NAVIGATION_FIX_END -->'''

          path.write_text(html.rstrip() + '\n' + patch + '\n', encoding='utf-8')
          PY

      - name: Publish final navigation
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data-search.html
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'Finalize DATA SEARCH navigation and actions'
          git push "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" gh-pages
