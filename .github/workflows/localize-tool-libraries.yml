# runtime restoration trigger 4
name: Restore full tool runtime

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/localize-tool-libraries.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout published tools
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          fetch-depth: 0

      - name: Download browser libraries into repository
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p vendor/images

          get_file() {
            local output="$1"; shift
            rm -f "$output"
            for url in "$@"; do
              echo "Downloading $url"
              if curl -fL --retry 3 --retry-delay 2 --connect-timeout 20 --max-time 180 "$url" -o "$output" && test -s "$output"; then
                return 0
              fi
            done
            echo "Unable to download $output" >&2
            exit 1
          }

          get_file vendor/leaflet.js \
            'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js' \
            'https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.js'
          get_file vendor/leaflet.css \
            'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css' \
            'https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.css'
          get_file vendor/leaflet.draw.js \
            'https://unpkg.com/leaflet-draw@1.0.4/dist/leaflet.draw.js' \
            'https://cdnjs.cloudflare.com/ajax/libs/leaflet.draw/1.0.4/leaflet.draw.js'
          get_file vendor/leaflet.draw.css \
            'https://unpkg.com/leaflet-draw@1.0.4/dist/leaflet.draw.css' \
            'https://cdnjs.cloudflare.com/ajax/libs/leaflet.draw/1.0.4/leaflet.draw.css'
          get_file vendor/turf.min.js \
            'https://cdn.jsdelivr.net/npm/@turf/turf@7.2.0/turf.min.js' \
            'https://unpkg.com/@turf/turf@7.2.0/turf.min.js'
          get_file vendor/jszip.min.js \
            'https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js' \
            'https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js'
          get_file vendor/shpwrite.js \
            'https://unpkg.com/shp-write@0.3.2/shpwrite.js' \
            'https://cdn.jsdelivr.net/npm/shp-write@0.3.2/shpwrite.js'
          get_file vendor/satellite.min.js \
            'https://cdn.jsdelivr.net/npm/satellite.js@6.0.1/dist/satellite.min.js' \
            'https://unpkg.com/satellite.js@6.0.1/dist/satellite.min.js'

          for image in layers.png layers-2x.png marker-icon.png marker-icon-2x.png marker-shadow.png; do
            get_file "vendor/images/$image" \
              "https://unpkg.com/leaflet@1.9.4/dist/images/$image" \
              "https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/images/$image"
          done
          for image in spritesheet.png spritesheet-2x.png; do
            get_file "vendor/images/$image" \
              "https://unpkg.com/leaflet-draw@1.0.4/dist/images/$image" \
              "https://cdnjs.cloudflare.com/ajax/libs/leaflet.draw/1.0.4/images/$image"
          done

      - name: Restore original runtime references and validate
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path

          replacements={
            'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css':'vendor/leaflet.css',
            'https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.css':'vendor/leaflet.css',
            'https://unpkg.com/leaflet-draw@1.0.4/dist/leaflet.draw.css':'vendor/leaflet.draw.css',
            'https://cdnjs.cloudflare.com/ajax/libs/leaflet.draw/1.0.4/leaflet.draw.css':'vendor/leaflet.draw.css',
            'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js':'vendor/leaflet.js',
            'https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.js':'vendor/leaflet.js',
            'https://unpkg.com/leaflet-draw@1.0.4/dist/leaflet.draw.js':'vendor/leaflet.draw.js',
            'https://cdnjs.cloudflare.com/ajax/libs/leaflet.draw/1.0.4/leaflet.draw.js':'vendor/leaflet.draw.js',
            'https://cdn.jsdelivr.net/npm/@turf/turf@7.2.0/turf.min.js':'vendor/turf.min.js',
            'https://unpkg.com/@turf/turf@7.2.0/turf.min.js':'vendor/turf.min.js',
            'https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js':'vendor/jszip.min.js',
            'https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js':'vendor/jszip.min.js',
            'https://unpkg.com/shp-write@0.3.2/shpwrite.js':'vendor/shpwrite.js',
            'https://cdn.jsdelivr.net/npm/shp-write@0.3.2/shpwrite.js':'vendor/shpwrite.js',
            'https://cdn.jsdelivr.net/npm/satellite.js@6.0.1/dist/satellite.min.js':'vendor/satellite.min.js',
            'https://unpkg.com/satellite.js@6.0.1/dist/satellite.min.js':'vendor/satellite.min.js',
          }

          for filename in ['aoi-tool.html','data-search.html']:
              p=Path(filename)
              s=p.read_text(encoding='utf-8')
              before=s
              for old,new in replacements.items():
                  s=s.replace(old,new)
              if s==before:
                  raise SystemExit(f'{filename}: no dependency URLs changed')
              p.write_text(s,encoding='utf-8')

          a=Path('aoi-tool.html').read_text(encoding='utf-8')
          d=Path('data-search.html').read_text(encoding='utf-8')
          assert 'src="vendor/leaflet.js"' in a and 'src="vendor/turf.min.js"' in a
          assert 'src="vendor/leaflet.js"' in d and 'src="vendor/satellite.min.js"' in d
          assert 'aoi-offline-map' in a
          assert 'Selected AOI area:' in d
          assert 'class="map-actions"' not in d
          blocked=['unpkg.com/leaflet','leaflet-draw@','@turf/turf@','cdnjs.cloudflare.com/ajax/libs/jszip','shp-write@','satellite.js@6.0.1']
          for name,text in [('AOI',a),('DATA SEARCH',d)]:
              for token in blocked:
                  assert token not in text, (name,token)
          for name in ['leaflet.js','leaflet.css','leaflet.draw.js','leaflet.draw.css','turf.min.js','jszip.min.js','shpwrite.js','satellite.min.js']:
              p=Path('vendor')/name
              assert p.exists() and p.stat().st_size>1000, (name,p.stat().st_size if p.exists() else 0)
          print('AOI bytes',Path('aoi-tool.html').stat().st_size)
          print('DATA SEARCH bytes',Path('data-search.html').stat().st_size)
          print('Local libraries ready')
          PY

      - name: Commit restored runtime
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add aoi-tool.html data-search.html vendor
          git diff --cached --check -- aoi-tool.html data-search.html
          git commit -m 'Restore full tool functionality with local map libraries'
          git push origin gh-pages
