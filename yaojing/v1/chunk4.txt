g.bases[1].hp<=0)finish(1);if(g.bases[0].hp<=0)finish(0);if(g.msgT>0){g.msgT-=dt}else msg.textContent='';bs.textContent=g.bs;rs.textContent=g.rs;lv.textContent=h.lv;gold.textContent=Math.floor(h.gold);kd.textContent=h.k+'/'+h.d;hpf.style.width=(100*clamp(h.hp,0,h.max)/h.max)+'%';xpf.style.width=(100*h.xp/80)+'%';time.textContent=String(Math.floor(g.time/60)).padStart(2,'0')+':'+String(Math.floor(g.time%60)).padStart(2,'0');dq.classList.toggle('cd',h.q>0);de.classList.toggle('cd',h.e>0);dr.classList.toggle('cd',h.r>0)}
function draw(){ctx.clearRect(0,0,1280,720);ctx.fillStyle='#163328';ctx.fillRect(0,0,1280,720);for(let i=0;i<45;i++){let x=(i*181)%1280,y=(i*113)%720;ctx.fillStyle=i%2?'#204732':'#143a2a';ctx.beginPath();ctx.arc(x,y,18+(i%5)*8,0,7);ctx.fill()}ctx.strokeStyle='#2d7184';ctx.lineWidth=62;ctx.beginPath();ctx.moveTo(390,-30);ctx.lineTo(900,750);ctx.stroke();for(let l=0;l<3;l++){let P=lanes[l].path;ctx.strokeStyle='#a4ad8d';ctx.lineWidth=64;ctx.beginPath();ctx.moveTo(P[0][0],P[0][1]);for(let p of P.slice(1))ctx.lineTo(p[0],p[1]);ctx.stroke();ctx.strokeStyle='#c6b9914a';ctx.lineWidth=40;ctx.stroke()}if(!G)return;for(let j of G.jungle)if(j.alive){ctx.fillStyle='#bb8cff';ctx.beginPath();ctx.arc(j.x,j.y,j.r,0,7);ctx.fill()}for(let t of G.towers){if(t.hp<=0)continue;ctx.fillStyle=t.team==='b'?'#5fb5ff':'#ff7077';ctx.fillRect(t.x-14,t.y-24,28,48);barDraw(t,48)}for(let b of G.bases){ctx.fillStyle=b.team==='b'?'#61b7ff':'#ff6b72';ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,7);ctx.fill();ctx.strokeStyle='#efd78c';ctx.lineWidth=3;ctx.stroke();barDraw(b,86)}let du=(u,c)=>{if(u.hp<=0)return;ctx.fillStyle=c;ctx.beginPath();ctx.arc(u.x,u.y,u.r,0,7);ctx.fill();barDraw(u,28)};G.allies.forEach(u=>du(u,'#70c1ff'));G.enemies.forEach(u=>du(u,'#ff8589'));G.heroes.forEach(u=>du(u,u.team==='b'?'#53ddff':'#ff555d'));if(G.h.resp<=0){ctx.fillStyle=G.h.buff>0?'#e6a7ff':'#f0d58b';ctx.beginPath();ctx.arc(G.h.x,G.h.y,G.h.r,0,7);ctx.fill();ctx.strokeStyle='#7bd7ff';ctx.lineWidth=4;ctx.stroke()}for(let s of G.shots){ctx.fillStyle='#e6f7ff';ctx.beginPath();ctx.arc(s.x,s.y,5,0,7);ctx.fill()}for(let f of G.fx){ctx.globalAlpha=clamp(f.life*2,0,1);ctx.strokeStyle=f.c;ctx.lineWidth=5;ctx.beginPath();ctx.arc(f.x,f.y,f.r,0,7);ctx.stroke();ctx.globalAlpha=1}}function barDraw(u,w){ctx.fillStyle='#0009';ctx.fillRect(u.x-w/2,u.y-(u.r||18)-12,w,5);ctx.fillStyle='#efd783';ctx.fillRect(u.x-w/2,u.y-(u.r||18)-12,w*clamp(u.hp/u.max,0,1),5)}function drawMini(){mx.clearRect(0,0,170,170);mx.fillStyle='#142e27';mx.fillRect(0,0,170,170);for(let l=0;l<3;l++){let P=lanes[l].path;mx.strokeStyle='#8c9e87';mx.lineWidth=6;mx.beginPath();mx.moveTo(P[0][0]*170/1280,P[0][1]*170/720);for(let p of P.slice(1))mx.lineTo(p[0]*170/1280,p[1]*170/720);mx.stroke()}if(!G)return;for(let t of G.towers){mx.fillStyle=t.team==='b'?'#65b9ff':'#ff6f75';mx.fillRect(t.x*170/1280-2,t.y*170/720-2,4,4)}if(G.h.resp<=0){mx.fillStyle='#f1d783';mx.beginPath();mx.arc(G.h.x*170/1280,G.h.y*170/720,4,0,7);mx.fill()}}function loop(t){let dt=Math.min(.033,(t-last)/1000||0);last=t;if(battle.classList.contains('active'))update(dt);draw();drawMini();requestAnimationFrame(loop)}requestAnimationFrame(loop);
const joy=$('joy'),stick=$('stick');let jid=null;function setJoy(t){let r=joy.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,dx=t.clientX-cx,dy=t.clientY-cy,m=42,l=Math.hypot(dx,dy)||1;if(l>m){dx=dx/l*m;dy=dy/l*m}tx=dx/m;ty=dy/m;stick.style.transform=`translate(${dx}px,${dy}px)`}function resetJoy(){jid=null;tx=ty=0;stick.style.transform=''}joy.addEventListener('touchstart',e=>{e.preventDefault();jid=e.changedTouches[0].identifier;setJoy(e.changedTouches[0])},{passive:false});joy.addEventListener('touchmove',e=>{e.preventDefault();for(let t of e.changedTouches)if(t.identifier===jid)setJoy(t)},{passive:false});joy.addEventListener('touchend',resetJoy,{passive:false});function bind(id,fn){$(id).addEventListener('touchstart',e=>{e.preventDefault();fn()},{passive:false});$(id).addEventListener('click',e=>{e.preventDefault();fn()})}bind('ma',attackNearest);bind('mq',skillQ);bind('me',skillE);bind('mr',skillR);bind('mrec',recall);buildShop();apply();openCreator();show('lobby');
</script></body></html>